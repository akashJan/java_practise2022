package W3_Source_Qns;
public class Ex8 {
    public static void main(String[] args) {


        // Q 27

//        Scanner scn = new Scanner(System.in);
//        System.out.println("Enter int element ");
//        int[] num = new int[5];
//
//        for (int i = 0; i < 5; i++) {
//            num[i] = scn.nextInt();
//        }
//        for (int element : num) {
//            System.out.println(element);
//        }
//
//        int total_num = 0;
//
//        for (int j = 0; j < num.length; j++) {
//            if (num[j] % 2 == 0)
//                total_num++;
//
//        }
//        System.out.println("total even number" + "\t" + total_num);
//        System.out.println("total odd number" + "\t" + (num.length - total_num));


        // Q 28
/*
        Scanner scn = new Scanner(System.in);
        System.out.println("Enter the length of array");
        int size = scn.nextInt();

        int [] array = new int[size];

        System.out.println("Enter the element");
        for (int i = 0; i < size; i++){
            array[i] = scn.nextInt();
        }
        System.out.println("Array elements are" + Arrays.toString(array));

        int max_value = array[0];
        int min_value = array[0];

        for(int i = 0; i < size; i++){
            if(array[i] > max_value )
                max_value = array[i];
            else if (array[i] < min_value)
                max_value = array[i];
        }

        System.out.println("Difference value of the largest value and smallest value of said array"+ "\t" + (max_value - min_value));
*/

    }
}
